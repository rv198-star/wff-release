# wff-impl-frontend Source Cards

Required source cards:

- P1 prototype or interaction flow contract when present.
- P2 frontend / interaction handoff when present.
- API documentation and backend route catalog.
- ActionCards for user-facing behavior.

If upstream UI truth is absent, generate only a bounded fallback or mark review-bound.
